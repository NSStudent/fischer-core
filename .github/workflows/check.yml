name: checks

on:
  push:
    branches: develop
  pull_request:
    branches: develop

jobs:
  check:
    uses: test-spm-codecov.yaml@master
    secrets: inherit
    with:
       test_bundle: ChessKitPackageTests